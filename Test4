steps:
- task: AzureCLI@2
  displayName: Push vars to AKV
  inputs:
    azureSubscription: 'my-azure-service-connection'
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
      set -e

      KV_NAME="my-kv-${{ parameters.environment }}"

      declare -A VAR_MAP
      VAR_MAP[db_jdbc_url]="db-jdbc-url"
      VAR_MAP[db_scope]="db-scope"
      VAR_MAP[api_key]="api-key"
      VAR_MAP[client_secret]="client-secret"

      for vg_var in "${!VAR_MAP[@]}"; do
        echo "Pushing $vg_var -> ${VAR_MAP[$vg_var]}"
        az keyvault secret set \
          --vault-name "$KV_NAME" \
          --name "${VAR_MAP[$vg_var]}" \
          --value "$(printenv $vg_var)"
      done
  env:
    db_jdbc_url: $(db_jdbc_url)
    db_scope: $(db_scope)
    api_key: $(api_key)
    client_secret: $(client_secret)
